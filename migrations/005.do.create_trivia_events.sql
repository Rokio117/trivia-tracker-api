

CREATE TABLE IF NOT EXISTS trivia_events (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  eventDate TEXT NOT NULL,
  eventLocation INTEGER REFERENCES trivia_locations(id) NOT NULL
)