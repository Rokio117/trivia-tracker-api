CREATE TABLE members(
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  player_id INTEGER NOT NULL REFERENCES trivia_players(id),
  team_id INTEGER NOT NULL REFERENCES trivia_teams(id)
);